{
	"name": "Ubuntu",
	"image": "mcr.microsoft.com/devcontainers/base:jammy",
	"remoteUser": "root",
	"postCreateCommand": "apt install -y git curl unzip zsh python3 && git clone https://ghproxy.com/https://github.com/sujianqingfeng/dotfiles.git && cd dotfiles && sh install && chsh -s /bin/zsh && curl -fsSL https://gitlab.com/hens/proxy/raw/main/fnm-install.sh | bash "

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},
	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],
	// Configure tool-specific properties.
	// "customizations": {},
}
