
- defaults:
    link:
      relink: true
      create: true

- clean: ['~']


- link: 
    ~/.zsh:
    ~/.zshrc:
    ~/powerlevel10k:
    ~/.p10k.zsh:
    ~/.tmux.conf:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
