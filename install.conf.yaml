
- defaults:
    link:
      relink: true
      create: true

- clean: ['~']


- link: 
    ~/.zsh:
    ~/.zshrc:
    ~/.oh-my-zsh:
    ~/.p10k.zsh:
    ~/.tmux.conf:
    ~/.config/nvim: nvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
