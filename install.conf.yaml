
- defaults:
    link:
      relink: true
      create: true

- clean: ['~']


- link: 
    ~/.bin:
    ~/.zsh:
    ~/.zshrc:
    ~/.oh-my-zsh:
    ~/.tmux.conf:
    ~/.config/nvim: nvim


- shell:
  - [git submodule update --init --recursive, Installing submodules]
