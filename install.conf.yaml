
- defaults:
    link:
      relink: true
      create: true

- clean: ['~']


- link: 
    ~/.bin:
    ~/.zsh:
    ~/.zshrc:
    ~/.oh-my-zsh:
    ~/.tmux.conf:
    ~/.tmux.conf.local:
    ~/.config/nvim: nvim


- shell:
  - [git submodule update --init --recursive, Installing submodules]
